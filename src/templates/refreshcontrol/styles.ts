import { StyleSheet } from 'react-native';

export const styles = {
  light: StyleSheet.create({
    refresh: {
      color: '<%= PrimaryColor %>',
    },
  }),
  dark: StyleSheet.create({
    refresh: {
      color: '#1C2526',
    },
  }),
};